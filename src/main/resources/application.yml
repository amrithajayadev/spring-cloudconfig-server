---
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amrithajayadev/spring-cloudconfig-tolls
          search-paths:
            - 'station*'
